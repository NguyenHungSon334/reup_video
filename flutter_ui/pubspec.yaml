name: reup_flutter
description: Douyin Reup — Flutter desktop UI

publish_to: 'none'
version: 2.1.27+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  web_socket_channel: ^3.0.0
  file_picker: ^8.0.0
  shared_preferences: ^2.2.0
  url_launcher: ^6.1.10
  package_info_plus: ^8.0.0

flutter:
  uses-material-design: true
