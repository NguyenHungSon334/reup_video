{
  "buildCommand": "git clone --depth 1 https://github.com/flutter/flutter.git -b stable && ./flutter/bin/flutter config --enable-web && cd flutter_ui && ../flutter/bin/flutter build web --release && cd .. && rm -rf flutter",
  "outputDirectory": "flutter_ui/build/web",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
