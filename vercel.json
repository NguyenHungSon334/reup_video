{
  "buildCommand": "git clone --depth 1 https://github.com/flutter/flutter.git -b stable && ./flutter/bin/flutter config --enable-web && cd flutter_ui && ../flutter/bin/flutter build web --release && cd .. && rm -rf flutter flutter_ui/.dart_tool flutter_ui/.pub-cache",
  "outputDirectory": "flutter_ui/build/web",
  "framework": null,
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}